MINT_VERSION=$(curl -s --disable-epsv --max-time 30 --list-only ftp://ftp.iinet.net.au/linux/linuxmint/stable/ | sort -n | tail -n 1)
REMOTE_URL="ftp://ftp.iinet.net.au/linux/linuxmint/stable/$MINT_VERSION/"
REMOTE_REGEX="linuxmint-(\d+)-mate-64bit(.*?).iso"
REMOTE_MD5="md5sums.txt"
